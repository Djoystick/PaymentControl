# Phase 25F - Support Flow Manual Checklist

Purpose: verify simplified donor-support flow after 25F without regressing 25B-25D safety/clarity.

## A) Main path clarity

1. Open Profile and expand `Support and Premium`.
2. Verify there is one obvious primary support action in the first visible support block.
3. Trigger the primary action and confirm external support opens.
4. Confirm messaging remains honest: manual review is still required.

Pass if:
- main path is obvious in one glance,
- no rail-strategy wall appears in the first lane,
- no automatic activation promise appears.

## B) Fallback path clarity

1. In the same support block, use `I already supported, submit claim`.
2. Verify claim panel opens quickly.
3. Verify at least one proof field requirement remains clear and compact.

Pass if:
- fallback is clearly available,
- fallback feels short and operational (not bureaucratic).

## C) Post-support continuation

1. Open external support from app and return.
2. Verify return block appears and `Continue to claim` is obvious.
3. Use `Continue to claim` and verify claim panel opens directly.

Pass if:
- return continuity feels natural,
- next action is immediately clear.

## D) Optional support reference and rail details

1. Open the optional support rails/details disclosure.
2. Verify Boosty/CloudTips state still renders honestly:
- configured rails open,
- unconfigured secondary rail shows calm pending state,
- no dead CTA.
3. Verify support reference code section still supports prepare/copy/refresh flow.

Pass if:
- optional details are available but non-dominant,
- operational rail truth is preserved.

## E) Owner/admin context preservation

1. Open owner claim queue (`PremiumAdminConsole`).
2. Verify rail-aware and continuity context still appears on claims.
3. Verify approve/reject flow remains unchanged and readable.

Pass if:
- owner queue did not lose rail-aware context,
- review ergonomics remain intact.

## F) RU/EN and honesty checks

1. Switch EN -> RU and re-check touched support flow blocks.
2. Confirm no subscription-first wording appears.
3. Confirm no fake automatic activation messaging appears.

Pass if:
- RU/EN remains coherent on touched strings,
- product honesty is preserved.

## G) Compact result log format

Use one line per group:
- `A main path: pass/fail - note`
- `B fallback: pass/fail - note`
- `C post-support: pass/fail - note`
- `D optional details: pass/fail - note`
- `E owner context: pass/fail - note`
- `F RU/EN honesty: pass/fail - note`

Overall:
- `Phase 25F manual verification: pass/fail`
